#include <stdio.h>

int main()
{
   int num,a,b;
   scanf("%d %d",&a,&b);
   for (num = a+1; num <= b; num++)
     {

               if (num % 2 == 1)

                  printf ("%d ", num);

     }

    return 0;
}
